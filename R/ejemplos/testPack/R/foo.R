#' foo function
#'
#' Esta funci\'on no hace otra cosa que devolver
#' el sucesor del numero, si el argumento es un n\'umero.
#' Si el argumento es un vector de n\'umeros, devuelce un
#' vector con sus sucesores.

foo <- function(x) x + 1
